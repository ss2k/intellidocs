<h1>Homepage</h1>
<%= link_to "New Upload", new_asset_path %>
<br />
Search
<%= form_tag root_path, method: 'get' do %>
  <%= text_field_tag :q %>
<% end %>
<% if @assets %>
  <% @assets.each do |a| %>
    <%= link_to a.asset.file.filename, a.asset.url %> |
    <%= a.notes %>
    <br/>
  <% end %>
<% end %>